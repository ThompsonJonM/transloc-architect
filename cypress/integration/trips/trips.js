import {
  Given,
  When,
  And,
  Then
} from 'cypress-cucumber-preprocessor/steps';

/**
 * Scenario: Create a New Trip
 *
 * @param create_or_delete           The type of action we are taking
 * @param created_or_deleted         The type of assertion we are passing
 */
Given('we are viewing the "Trips" tab for a specific feed', () => {

});

And('we have existing routes, patterns, and calendars', () => {

});

When('we select to {string} a trip', (create_or_delete) => {

});

Then('our trip should be successfully {string}', (created_or_deleted) => {

})